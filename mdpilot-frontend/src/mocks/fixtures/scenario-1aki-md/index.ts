import chat from './chat.json' with { type: 'json' };
import messages from './messages.json' with { type: 'json' };

export const aki1Scenario = { chat, messages, tasks: [], artifacts: [], toolCalls: [] };
